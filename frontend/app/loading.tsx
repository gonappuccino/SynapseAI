export default function Loading() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-[var(--bg)]">
      <p className="text-[11px] uppercase tracking-[0.2em] text-gray-400">Loading...</p>
    </main>
  );
}
